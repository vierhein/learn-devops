apiVersion: v1
kind: Service
metadata:
  name: proxy-service
spec:
  selector:
    {{ .Values.labelsNginx.key }}: {{ .Values.labelsNginx.value }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort