apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "task2-chart.fullname" . }}-test-connection"
  labels:
    {{- include "task2-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "task2-chart.fullname" . }}:{{ .Values.serviceNginx.port }}']
  restartPolicy: Never
