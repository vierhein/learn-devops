apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.deploymentNameNginx }}
  labels:
    {{ .Values.labelsNginx.key }}: {{ .Values.labelsNginx.value }}
spec:
  replicas: {{ .Values.replicaCountNginx }}
  strategy:
    type: {{ .Values.typeDeploymentStrategy }}
  selector:
    matchLabels:
      {{ .Values.labelsNginx.key }}: {{ .Values.labelsNginx.value }}
  template:
    metadata:
      labels:
        {{ .Values.labelsNginx.key }}: {{ .Values.labelsNginx.value }}
    spec:
      containers:
        - name: {{ .Values.imageNginx.containerName }}
          image: "{{ .Values.imageNginx.repository }}:{{ .Values.imageNginx.tag }}"
          ports:
            - containerPort: 80
          volumeMounts:
            - name: {{ .Values.volumeNginxName }}
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
              readOnly: true
      volumes:
        - name: {{ .Values.volumeNginxName }}
          configMap:
            name: {{ .Values.configMapNginxName }}